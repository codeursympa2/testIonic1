import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.testIonic1.app',
  appName: 'testIonic1 App',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
